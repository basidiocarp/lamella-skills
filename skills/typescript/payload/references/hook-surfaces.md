# Hook Surfaces

Use this reference for choosing between collection hooks, field hooks, and lifecycle phases.

- `beforeValidate` for shaping input
- `beforeChange` for business logic
- `afterChange` for side effects
- `afterRead` for computed presentation fields
