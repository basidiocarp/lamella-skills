# Trait Basics and Dispatch

Use this reference for trait bounds, associated types, and trait-object tradeoffs.

- Prefer static dispatch by default
- Use trait objects when heterogeneity or runtime composition matters
- Keep bounds readable at the call site
