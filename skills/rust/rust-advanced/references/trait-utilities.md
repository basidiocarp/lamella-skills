# Trait Utilities

Use this reference for derive macros, extension traits, associated constants, and marker traits.

- Derive standard traits when they match semantics
- Use extension traits to add methods without changing the base type
- Keep marker traits and associated constants explicit and well-scoped
