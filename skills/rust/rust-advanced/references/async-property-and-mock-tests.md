# Async, Property, and Mock Tests

Use this reference when ordinary unit tests are not enough.

- `tokio::test` or equivalent for async behavior
- `proptest` for invariant-heavy logic
- mocking only at real dependency seams
