# Rust Project Layout Reference

Relevant source rule groups:

- `resources/rules/rust/proj-*.md`
- `resources/rules/rust/coding-style.md`

Use this reference for:

- crate vs workspace boundaries
- `lib.rs` vs `main.rs`, `src/bin`, prelude modules, module-by-feature structure
- `pub(crate)`, `pub(super)`, re-exports, internal API visibility
- workspace dependency and shared lint policy
