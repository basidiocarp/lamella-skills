# Rust Testing Patterns Reference

Relevant source rule group:

- `resources/rules/rust/test-*.md`

Use this reference for:

- `#[cfg(test)]` vs `tests/`
- doctests, async tests, property tests, Criterion benchmarks
- mocks vs traits vs fakes
- fixture cleanup, test names, panic tests, and AAA structure
