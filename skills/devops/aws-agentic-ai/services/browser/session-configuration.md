# Browser Session Configuration

- Set viewport, timeout, and region explicitly for reproducibility.
- Use headless defaults unless a visual workflow truly needs rendering.
- Apply resource and session limits to prevent runaway browser work.
