# Command Templating and Nesting

## Templating

- use arguments and flags in step templates explicitly
- keep template expressions readable enough to debug

## Nested Commands

- use nesting when subcommands form one clear family
- keep sibling commands unique and predictable on the CLI
