# Command Arguments and Flags

## Arguments

- positional, ordered, and usually accessed through template variables
- use `required` and sensible defaults deliberately

## Flags

- named inputs with optional shorthand
- use explicit boolean types for boolean flags
- document defaults and required flags clearly
