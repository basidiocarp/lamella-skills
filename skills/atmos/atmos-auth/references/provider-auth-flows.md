# Provider Auth Flows

Use this reference for provider-specific login flow behavior.

- AWS SSO and device-code flows behave differently from static credentials
- Browser-based flows should be documented separately from CLI-only auth
- Keep provider notes close to the operational command that needs them
