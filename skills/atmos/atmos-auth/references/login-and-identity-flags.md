# Login and Identity Flags

Use this reference for `atmos auth login` and identity-selection behavior.

- `--identity` with a value selects an identity
- bare `--identity` forces interactive choice
- omitted identity uses defaults or prompts
