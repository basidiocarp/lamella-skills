# Paths, Stacks, and Schemas

This slice covers where Atmos finds things.

## Main Areas

- `base_path`
- `stacks`
- `workflows`
- `schemas`

## Rule

Treat path discovery and naming conventions as foundational config. If stack
discovery is wrong, every downstream command becomes noisy or misleading.
