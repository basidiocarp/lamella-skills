# Atmos Template Functions

Atmos-specific helpers include:

- `atmos.Component`
- `atmos.GomplateDatasource`
- `atmos.Store`

Use them when cross-component data, external datasources, or configured stores
belong in the rendered configuration.
