# Sprig and Gomplate Functions

Use Sprig and Gomplate helpers for:

- string manipulation
- list and map operations
- type conversion
- environment access

Keep function choice explicit when both libraries are enabled so the template
remains readable and debuggable.
