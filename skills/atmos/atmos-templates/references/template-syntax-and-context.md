# Template Syntax and Context

Use Go template delimiters around values and expressions that must render before
YAML parsing.

Context commonly includes:

- component and stack identifiers
- `vars`
- `settings`
- `metadata`
- backend and provider configuration
