# Vendor Manifest

Use this reference for the shape of `vendor.yaml`.

- declare sources explicitly
- keep versions or refs pinned
- separate source metadata from local destination rules
- treat vendoring as reproducible input, not an ad hoc fetch step
