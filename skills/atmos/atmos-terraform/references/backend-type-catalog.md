# Backend Type Catalog

Common backend types include:

- `s3`
- `gcs`
- `azurerm`
- `remote`
- other Terraform-supported backends such as `consul`, `pg`, or `http`

Pick the backend based on the platform and operational controls you actually
need, not just habit.
