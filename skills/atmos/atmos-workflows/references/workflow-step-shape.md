# Workflow Step Shape

Use this reference for per-step syntax and defaults.

- Set shared defaults at the workflow level when they truly apply broadly
- Override stack or working directory only where needed
- Keep step logic simple enough to reason about from YAML alone
