# Devcontainer Commands Reference

- `start` to create or boot the environment
- `attach` or `shell` for interactive work
- `exec` for one-off commands
- `rebuild` when the container definition changes materially
