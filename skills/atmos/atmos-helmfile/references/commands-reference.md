# Helmfile Commands Reference

Use this reference to choose the right Atmos Helmfile command.

- `sync` for convergence
- `apply` when interactive review is part of the flow
- `destroy` only with explicit teardown intent
- `diff` before risky changes
