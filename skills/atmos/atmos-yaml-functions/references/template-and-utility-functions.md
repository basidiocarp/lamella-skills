# Template and Utility Functions

Use this slice for helper-style YAML function usage that depends on template
evaluation or small utility behavior rather than external system lookups.

## Rule

Keep template expansion understandable. If a stack file reads like a programming
language instead of configuration, the function layer is doing too much.
