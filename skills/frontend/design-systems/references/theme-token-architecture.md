# Theme Token Architecture

Build theming on CSS custom properties and semantic tokens.

## Rules

- define a stable token contract first
- map semantic tokens to light, dark, or brand-specific values
- use tokens in components instead of hardcoded colors or shadows
