# Theme Provider and Toggle

Use a provider when the app needs:

- explicit theme switching
- system-theme support
- persisted user preference

Keep the toggle simple: light, dark, and system are usually enough.
