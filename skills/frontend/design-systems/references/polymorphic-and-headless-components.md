# Polymorphic and Headless Components

## Polymorphic Components

- use when one component must render as different element types
- keep typing tight so consumers do not lose prop safety

## Headless Components

- use when behavior should be reusable across many visual treatments
- keep styling concerns outside the headless primitive
