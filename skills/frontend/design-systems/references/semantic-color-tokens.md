# Semantic Color Tokens

Map raw palette values into semantic roles such as:

- background
- text
- border
- interactive
- success, warning, and error

## Rule

Consumers should mostly depend on semantic tokens, not on raw palette stops,
unless they are building the design system itself.
