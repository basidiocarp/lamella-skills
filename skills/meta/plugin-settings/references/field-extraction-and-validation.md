# Field Extraction and Validation

## Extraction

- support strings, booleans, numbers, and optional values explicitly
- give defaults in code instead of relying on missing-field behavior
- use a real YAML tool for arrays or nested structures when available

## Validation

- check file existence and readability
- verify frontmatter markers exist before parsing
- validate enumerations and numeric ranges before acting on values
