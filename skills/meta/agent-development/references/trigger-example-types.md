# Trigger Example Types

Use examples from several trigger modes:

- explicit request
- proactive follow-up after relevant work
- implicit request based on user pain
- tool-usage or file-change pattern

Cover multiple phrasings for the same intent, but keep each example focused on
one decision boundary.
