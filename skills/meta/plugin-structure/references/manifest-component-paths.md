# Manifest Component Paths

Use component path fields when a plugin needs non-default locations for its
resources.

Examples:

- `commands`
- `agents`
- `skills`
- `hooks`
- `rules`

## Rules

- supplement default directories deliberately
- keep path layout predictable
- prefer a few clear category paths over many tiny special cases
