# Plugin Command Analysis Pattern

Good plugin analysis commands usually:

- use `${CLAUDE_PLUGIN_ROOT}` for scripts and templates
- keep scope to one input target
- return structured findings, not raw tool noise
