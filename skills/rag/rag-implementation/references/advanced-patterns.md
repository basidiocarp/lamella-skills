# Advanced RAG Patterns

Use advanced patterns only after the base pipeline is already evaluated.

Examples:

- hybrid search with reciprocal-rank fusion
- multi-query retrieval
- contextual compression
- parent-document retrieval
- HyDE, self-query, or corrective retrieval

Each pattern improves a different failure mode. Add them based on measured recall
or precision gaps, not by default.
