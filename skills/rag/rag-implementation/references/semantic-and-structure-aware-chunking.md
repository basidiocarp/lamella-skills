# Semantic and Structure-Aware Chunking

Use semantic or document-aware chunking when boundaries matter more than byte
count.

Good fits:

- technical docs
- manuals
- structured markdown or HTML
- code-aware retrieval

These approaches cost more than naive splitting but usually improve retrieval
precision.
