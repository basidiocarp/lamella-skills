# Embedding Optimization and Operations

Focus on:

- preprocessing and normalization
- embedding cache strategy
- dimension reduction trade-offs
- fine-tuning only when the corpus justifies it

Do not fine-tune before you have retrieval-quality evidence and hard negatives
that show the base model is not enough.
