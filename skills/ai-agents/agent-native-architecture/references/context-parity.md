# Context Parity

The agent should see the same important state the user sees.

Check:

- current resources
- recent activity
- app vocabulary
- action/tool mapping

If the UI shows it and the agent cannot access it, parity is incomplete.
