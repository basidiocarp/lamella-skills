# Starting with Primitives

Start with atomic capabilities such as file operations, storage, shell access,
and HTTP requests.

Why:

- proves the architecture works
- exposes real user demand
- avoids premature tool design
- forces prompts to carry the behavior
