# Tool Parity Design

Design agent tools at roughly the same conceptual granularity as the UI.

## Rules

- use user vocabulary in tool names
- prefer one conceptual action per tool
- return the same kind of confirmation the UI would show
- do not hide common user actions behind technical jargon
