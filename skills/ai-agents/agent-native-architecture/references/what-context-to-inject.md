# What Context to Inject

Inject the state the user expects the agent to know about:

- available resources
- current or recent activity
- capability-to-language mapping
- domain vocabulary

If the user can see it and the agent needs it to act correctly, it probably
belongs in runtime context.
