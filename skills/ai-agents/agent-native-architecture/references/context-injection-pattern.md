# Context Injection Pattern

Build the system prompt from:

- base agent instructions
- current app state
- recent activity
- capability and vocabulary mapping

Implement this through a context builder, provider set, or template renderer.
